# ============================================================
# YokeBot Engine — Environment Variables
# ============================================================
# Copy this file to .env and fill in your values.
# All variables are optional unless marked REQUIRED.
# ============================================================

# ---- Server ----
PORT=3001
# YOKEBOT_DATA_DIR=~/.yokebot          # Where agent state + SQLite DB live
# YOKEBOT_WORKSPACE_DIR=                # Workspace files (default: DATA_DIR/workspace)
# YOKEBOT_SKILLS_DIR=../../skills       # Path to skills directory

# ---- Database ----
# If set, uses Postgres. If not set, uses local SQLite (no setup needed).
# DATABASE_URL=postgresql://user:pass@localhost:5432/yokebot

# ---- Auth (Supabase) ----
# Required for multi-user auth. Not needed for local single-user usage.
# SUPABASE_JWT_SECRET=your-jwt-secret
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your-anon-key

# ---- CORS ----
# Comma-separated origins allowed to call the API.
CORS_ALLOWED_ORIGINS=http://localhost:5173

# ---- LLM & Media Providers ----
# Configure at least one LLM provider for your agents to use.
# DEEPINFRA_API_KEY=your-key                 # Primary LLM provider
# OPENROUTER_API_KEY=your-key                # Secondary LLM provider
# FAL_API_KEY=your-key                       # Media generation (image, video, 3D, audio)

# Fallback LLM endpoint (used if no provider configured for a model)
# YOKEBOT_FALLBACK_ENDPOINT=https://your-provider/v1
# YOKEBOT_FALLBACK_MODEL=your-model-name
# YOKEBOT_FALLBACK_API_KEY=your-key

# ---- Skills / Integrations ----
# API keys for built-in skills. Only needed if your agents use these skills.
# TAVILY_API_KEY=your-tavily-key             # Web search
# BRAVE_API_KEY=your-brave-key               # Web search (alternative)

# ---- Credential Encryption ----
# 32-byte hex key for encrypting stored credentials (API keys, tokens).
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# If not set, credentials are stored in plaintext.
# YOKEBOT_ENCRYPTION_KEY=your-64-char-hex-key
